{% extends 'masterChats' %}

{%% block chats %}

{{ form.open({action: 'ChatsController.store'}) }}

    {{ csrfField }}

    <div class="container">
      <div class="form-group">
        {{ form.label('message', 'Message') }}
        {{ form.text('message','',{class:'form-control'}) }}
      </div>

      <div class="form-group">
        {{ form.submit('Create') }}
      </div>

    {{ form.close() }}
    </div>


{% endblock %}
